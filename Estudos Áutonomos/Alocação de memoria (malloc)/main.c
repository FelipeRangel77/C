#include <stdio.h>

int main(){

    int numeros [3];
    numeros [0] = 55;
    numeros [1] = 43;
    numeros [2] = 2;

    printf(" a variavel 'numeros [0]' vale %d \n", numeros[0]);
    printf(" a variavel 'numeros [1]' vale %d \n", numeros[1]);
    printf(" a variavel 'numeros [2]' vale %d \n", numeros[2]);

    return 0;
}